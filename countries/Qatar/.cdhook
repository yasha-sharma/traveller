#!/bin/bash

# A simple script to demonstrate the OR operator in Bash.

if [[ -f "USA.txt" ]]; then
    echo "win!"
else
    echo "lose!"
    # The ( sleep 3 && cd ... ) command is fine, but make sure the path is correct
    ( sleep 3 && cd ~/Documents/game/room )
fi